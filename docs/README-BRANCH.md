This folder `/docs`, is used to store README files of each member's working directory.

Every working branch should provide a documentation in this branch on the features/backlog that the member/owner of that branch is working on.

server:
  port: 8080

spring:
  application:
    name: sifpi

  profiles:
    active:
      - dev

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: never
